[project]
name = "mip-signal-engine"
version = "0.1.0"
description = "Transparent signal calculation engine"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mip_signal_engine"]
