[project]
name = "mip-provenance"
version = "0.1.0"
description = "Provenance tracking utilities"
requires-python = ">=3.12"
dependencies = [
    "mip-schemas",
]

[tool.uv.sources]
mip-schemas = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/mip_provenance"]
